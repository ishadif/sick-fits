type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  price: Int!
  image: String
  largeImage: String
  createdAt: DateTime!
  updatedAt: DateTime!
  # user: User!
}
